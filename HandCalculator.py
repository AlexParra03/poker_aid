class Plays:
    def __init__(self, distance, playTye, cardsInPlay, cardsMissing):
        self.distance = distance
        self.playType = playType
        self.cardsInPlay = cardsInPlay
        self.cardsMissing = cardsMissing

def pair_distance(cardsOnHand, cardsOnTable):
    plays = []
    for cardOnTable in cardsOnTable:
        for cardOnHand in cardsOnHand:
            if cardOnHand.number == cardOnHand.number:
                plays.append(Play(0, "PAIR", [cardOnHand, cardOnTable], [])
            else playss


        
    
